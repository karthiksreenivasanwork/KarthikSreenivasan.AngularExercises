import { FirstletteruppercasePipe } from './firstletteruppercase.pipe';

describe('FistletteruppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstletteruppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
