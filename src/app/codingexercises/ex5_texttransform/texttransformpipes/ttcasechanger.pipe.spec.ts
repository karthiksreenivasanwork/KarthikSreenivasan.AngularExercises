import { TtCaseChangerPipe } from "./ttcasechanger.pipe";


describe('TttitlecasePipe', () => {
  it('create an instance', () => {
    const pipe = new TtCaseChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
