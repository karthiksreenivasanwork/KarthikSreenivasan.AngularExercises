import { TtcleanwordPipe } from './ttcleanword.pipe';

describe('TtcleanwordPipe', () => {
  it('create an instance', () => {
    const pipe = new TtcleanwordPipe();
    expect(pipe).toBeTruthy();
  });
});
