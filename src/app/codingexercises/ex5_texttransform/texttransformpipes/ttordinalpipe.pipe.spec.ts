import { TtordinalpipePipe } from './ttordinalpipe.pipe';

describe('TtordinalpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TtordinalpipePipe();
    expect(pipe).toBeTruthy();
  });
});
