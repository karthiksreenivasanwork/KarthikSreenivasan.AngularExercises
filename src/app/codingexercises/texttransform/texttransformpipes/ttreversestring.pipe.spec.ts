import { TtreversestringPipe } from './ttreversestring.pipe';

describe('TtreversestringPipe', () => {
  it('create an instance', () => {
    const pipe = new TtreversestringPipe();
    expect(pipe).toBeTruthy();
  });
});
