import { CardimageselectorPipe } from './cardimageselector.pipe';

describe('CardimageselectorPipe', () => {
  it('create an instance', () => {
    const pipe = new CardimageselectorPipe();
    expect(pipe).toBeTruthy();
  });
});
