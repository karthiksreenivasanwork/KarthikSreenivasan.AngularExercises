import { CardnumbermaskPipe } from './cardnumbermask.pipe';

describe('CardnumbermaskPipe', () => {
  it('create an instance', () => {
    const pipe = new CardnumbermaskPipe();
    expect(pipe).toBeTruthy();
  });
});
